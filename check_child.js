const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findUnique({ where: { email: 'sobuj@gmail.com' }, include: { children: { orderBy: { createdAt: 'desc' } } } }); console.log('CHILDREN_JSON:', JSON.stringify(user.children, null, 2)); } main().catch(e => console.error(e)).finally(() => prisma.$disconnect());
